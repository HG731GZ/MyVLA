# 当前版本训练指令

当前代码只保留一种动作头：

```text
9层 × 768维 × 12头
CrossAttention(VLM memory) -> SelfAttention(action chunk) -> MLP
```

第一轮实验只输入两张图：

```text
AgentView + WristView
```

训练数据为 `LIBERO_OBJECT`。参考图、四图角色编码和参考图 dropout 暂未启用。

## 单卡训练

```bash
conda activate simvla

# 如果训练机上的仓库路径不同，请修改这一行
cd /home/hg731/HG/MyVLA

export PYTHONNOUSERSITE=1
export CUDA_VISIBLE_DEVICES=0
export NUM_PROCESSES=1
export NUM_WORKERS=8
export GRADIENT_ACCUMULATION_STEPS=1
export SAVE_INTERVAL=20000
export LOG_INTERVAL=10
export ITERS=100000

export SMOLVLM_MODEL=./pretrained/SmolVLM-500M-Instruct
export VLM_TORCH_DTYPE=float32
export LEARNING_RATE=1e-4

bash train_smolvlm_small.sh \
    32 \
    0.1 \
    ./runs/libero_object_cross_self
```

三个位置参数依次是：

```text
32                                每个 GPU/process 的 batch size
0.1                               VLM 学习率系数
./runs/libero_object_cross_self   输出目录
```

实际学习率：

```text
动作头学习率 = 1e-4
VLM 解冻后学习率 = 1e-4 × 0.1 = 1e-5
```

VLM 前 1000 个 optimizer step 冻结，新动作头从第 1 步开始完整训练。

如果显存不足，将 batch size 从 `32` 降到 `16` 或 `8`。也可以增加：

```bash
export GRADIENT_ACCUMULATION_STEPS=2
```

## 留一任务训练

不需要删除或移动原始 HDF5。通过文件名排除任务，并关闭官方完整集计数校验：

```bash
export LIBERO_STRICT_VALIDATION=0
export LIBERO_EXCLUDE_FILES="pick_up_the_milk_and_place_it_in_the_basket_demo.hdf5"
```

然后正常运行训练命令：

```bash
bash train_smolvlm_small.sh \
    32 \
    0.1 \
    ./runs/libero_object_cross_self_without_milk
```

可以使用空格分隔多个文件名或 glob：

```bash
export LIBERO_EXCLUDE_FILES="*milk*_demo.hdf5 *ketchup*_demo.hdf5"
```

排除 pattern 如果没有匹配任何 HDF5，脚本会直接报错，避免误把目标任务保留在训练集。

恢复完整数据训练时：

```bash
unset LIBERO_EXCLUDE_FILES
export LIBERO_STRICT_VALIDATION=1
```

不要手工修改 `libero_object_train.json` 来删除任务，因为训练前的自动检查会根据 HDF5 数据重新生成
该文件。

## 自动刷新派生文件

每次启动训练前，脚本都会计算当前训练数据的指纹。指纹包含：

```text
subset
排除规则
HDF5 相对路径
文件大小和修改时间
demo 数
timestep 数
```

如果当前数据与以下文件不匹配，脚本会自动重新生成：

```text
datasets/metas/libero_object_train.json
norm_stats/libero_object_norm.json
```

数据和配置没有变化时不会重复计算 norm stats。需要无条件重建时：

```bash
export LIBERO_FORCE_REBUILD=1
```

重建完成后建议恢复：

```bash
unset LIBERO_FORCE_REBUILD
```

## 四卡训练

```bash
conda activate simvla
cd /home/hg731/HG/SimVLA-HG/SimVLA

export PYTHONNOUSERSITE=1
export CUDA_VISIBLE_DEVICES=0,1,2,3
export NUM_PROCESSES=4
export NUM_WORKERS=8
export GRADIENT_ACCUMULATION_STEPS=1

export SMOLVLM_MODEL=./pretrained/SmolVLM-500M-Instruct
export VLM_TORCH_DTYPE=float32
export LEARNING_RATE=1e-4

bash train_smolvlm_small.sh \
    32 \
    0.1 \
    ./runs/libero_object_cross_self
```

此时有效全局 batch size 为：

```text
32 × 4 GPU × 1 梯度累积 = 128
```

## 修改训练步数

默认训练 `800000` 个 optimizer step。通过环境变量修改：

```bash
export ITERS=400000
```

常用可调项：

```bash
export SAVE_INTERVAL=20000
export LOG_INTERVAL=20
export FREEZE_VLM_STEPS=1000
export WARMUP_STEPS=0
```

## 从新结构 checkpoint 继续训练

`ITERS` 表示最终总步数，不是额外增加的步数。

```bash
conda activate simvla
cd /home/hg731/HG/SimVLA-HG/SimVLA

export PYTHONNOUSERSITE=1
export CUDA_VISIBLE_DEVICES=0
export NUM_PROCESSES=1
export NUM_WORKERS=8
export GRADIENT_ACCUMULATION_STEPS=1

export SMOLVLM_MODEL=./pretrained/SmolVLM-500M-Instruct
export VLM_TORCH_DTYPE=float32
export LEARNING_RATE=1e-4
export ITERS=800000

bash train_smolvlm_small.sh \
    32 \
    0.1 \
    ./runs/libero_object_cross_self \
    ./runs/libero_object_cross_self/ckpt-200000
```

只能加载当前 Cross-Self 结构产生的 checkpoint。旧动作头 checkpoint 不再兼容。
